# Gold Research Terminal — runtime dependencies
# Pinned to ranges that resolve to manylinux wheels on Streamlit Community Cloud.

streamlit>=1.45,<2
pandas>=2.1
numpy>=1.26,<2.3
scipy>=1.11
statsmodels>=0.14
arch>=6.3            # GARCH volatility modelling
plotly>=5.20
pyarrow>=15          # parquet snapshot I/O
requests>=2.31
anthropic>=0.40      # optional, grounded narrative layer
# Research-note export is dependency-free HTML/Markdown (print-to-PDF in browser).
